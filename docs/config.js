/**
 * RideNDine Configuration
 * Global configuration for frontend application
 * 
 * To configure API base URL for GitHub Pages deployment:
 * Set apiBaseUrl to your Railway backend URL (e.g., 'https://ridendine-demo-production.up.railway.app')
 * 
 * For same-origin deployment (Railway serves /docs):
 * Leave apiBaseUrl as empty string ''
 */

window.__RIDENDINE_CONFIG__ = {
  // API Base URL
  // For GitHub Pages: Set to your Railway backend URL
  // For same-origin: Leave as '' (empty string)
  // Example: 'https://ridendine-demo-production.up.railway.app'
  apiBaseUrl: ''
};
