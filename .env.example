# Public Supabase (shared across apps)
NEXT_PUBLIC_SUPABASE_URL=
NEXT_PUBLIC_SUPABASE_ANON_KEY=

# Stripe server secrets (only for webhook/API app; never NEXT_PUBLIC)
STRIPE_SECRET_KEY=
STRIPE_WEBHOOK_SECRET=

# Optional admin gate
NEXT_PUBLIC_ADMIN_MASTER_PASSWORD=admin123
