# Home Chef Delivery - Environment Variables Reference
#
# ⚠️ DO NOT COPY THIS FILE DIRECTLY
# This is a reference guide showing all environment variables used across the monorepo.
# Use the app-specific .env.example files instead:
#   - apps/admin/.env.example → apps/admin/.env.local
#   - apps/mobile/.env.example → apps/mobile/.env
#   - backend/supabase/.env.example → backend/supabase/.env

# ============================================
# Admin Dashboard
# File: apps/admin/.env.local
# ============================================
# Supabase Configuration (get from https://supabase.com/dashboard)
NEXT_PUBLIC_SUPABASE_URL=https://your-project.supabase.co
NEXT_PUBLIC_SUPABASE_ANON_KEY=your-anon-key

# Dev-only auth bypass (Development only)
# WARNING: DEV_AUTH_BYPASS skips authentication. NEVER use in production!
DEV_AUTH_BYPASS=false

# ============================================
# Mobile App
# File: apps/mobile/.env
# ============================================
# Supabase Configuration
EXPO_PUBLIC_SUPABASE_URL=https://your-project.supabase.co
EXPO_PUBLIC_SUPABASE_ANON_KEY=your-anon-key

# Google Maps (for customer and driver location features)
EXPO_PUBLIC_GOOGLE_MAPS_API_KEY=your-google-maps-api-key

# ============================================
# Supabase Edge Functions
# File: backend/supabase/.env
# ============================================
# Stripe Configuration (for payment processing)
STRIPE_SECRET_KEY=sk_test_...
STRIPE_PUBLISHABLE_KEY=pk_test_...
STRIPE_WEBHOOK_SECRET=whsec_...

# Application URLs
APP_BASE_URL=https://your-app-domain.com

# ============================================
# Application Configuration
# ============================================
NODE_ENV=development

# ============================================
# Setup Instructions
# ============================================
# 1. Copy each app-specific .env.example file to its corresponding .env file:
#    cp apps/admin/.env.example apps/admin/.env.local
#    cp apps/mobile/.env.example apps/mobile/.env
#    cp backend/supabase/.env.example backend/supabase/.env
#
# 2. Fill in your actual values from Supabase, Stripe, etc.
#
# 3. NEVER commit .env or .env.local files to git (already in .gitignore)
